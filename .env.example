# Copy this file to .env and fill in your secrets. Do NOT commit .env.
# Database connection (used by valuation-service)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/valuation

# Note: Provider credentials (Finnhub API key, Webhook secret, API/WS URLs)
# are managed at runtime via admin endpoints and stored in the database.
# Do NOT set FINNHUB_API_KEY or WEBHOOK_SECRET here anymore.
# Configure via the dashboard Settings page or:
#   GET  /api/admin/provider-config
#   PUT  /api/admin/provider-config  { api_key, webhook_secret }
